<nav class="navbar navbar-expand-lg navbar-dark"
     id="mainNav"
     data-controller="navbar-scroll active-section">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('home') }}">
            <span class="navbar-logo-badge">
                <img src="{{ asset('img/logo/van_moer_logistics_cycling_team.png') }}"
                     alt="{{ 'app.name'|trans }}"
                     title="{{ 'app.name'|trans }}"
                     height="50" />
            </span>
        </a>
        <button class="navbar-toggler" type="button"
                data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false"
                aria-label="Toggle navigatie">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <twig:Site:Menu />
            <div class="d-flex ms-lg-3 gap-2 mt-3 mt-lg-0">
                <a href="https://www.facebook.com/wielerteamwaasland" target="_blank" rel="noopener"
                   class="btn btn-outline-light btn-sm" aria-label="Facebook">
                    <twig:ux:icon name="bi:facebook" />
                </a>
                <a href="https://www.instagram.com/van_moer_cycling_team_/" target="_blank" rel="noopener"
                   class="btn btn-outline-light btn-sm" aria-label="Instagram">
                    <twig:ux:icon name="bi:instagram" />
                </a>
            </div>
        </div>
    </div>
</nav>
